import { listActivities, updateActivity, createActivity, countActivities, searchActivities } from './_activities'
import { searchPatients, createPatient } from './_patients'

const Services = {
	listActivities,
	updateActivity,
	createActivity,
	searchPatients,
	createPatient,
	countActivities,
	searchActivities
}

export default Services